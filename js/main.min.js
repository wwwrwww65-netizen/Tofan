var $jscomp = $jscomp || {};
(($jscomp.scope = {}),
  ($jscomp.createTemplateTagFirstArg = function (e) {
    return (e.raw = e);
  }),
  ($jscomp.createTemplateTagFirstArgWithRaw = function (e, t) {
    return ((e.raw = t), e);
  }),
  ($jscomp.arrayIteratorImpl = function (e) {
    var t = 0;
    return function () {
      return t < e.length ? { done: !1, value: e[t++] } : { done: !0 };
    };
  }),
  ($jscomp.arrayIterator = function (e) {
    return { next: $jscomp.arrayIteratorImpl(e) };
  }),
  ($jscomp.makeIterator = function (e) {
    var t =
      "undefined" != typeof Symbol && Symbol.iterator && e && e[Symbol.iterator];
    return t ? t.call(e) : $jscomp.arrayIterator(e || []);
  }),
  ($jscomp.arrayFromIterator = function (e) {
    for (var t, n = []; !(t = e.next()).done;) n.push(t.value);
    return n;
  }),
  ($jscomp.arrayFromIterable = function (e) {
    return e instanceof Array
      ? e
      : $jscomp.arrayFromIterator($jscomp.makeIterator(e));
  }),
  ($jscomp.checkStringArgs = function (e, t, n) {
    if (null == e)
      throw new TypeError(
        "The 'this' value for String.prototype." +
          n +
          " must not be null or undefined",
      );
    if (t instanceof RegExp)
      throw new TypeError(
        "First argument to String.prototype." +
          n +
          " must not be a regular expression",
      );
    return e + "";
  }),
  ($jscomp.ASSUME_ES5 = !1),
  ($jscomp.ASSUME_NO_NATIVE_MAP = !1),
  ($jscomp.ASSUME_NO_NATIVE_SET = !1),
  ($jscomp.SIMPLE_FROUND_POLYFILL = !1),
  ($jscomp.ISOLATE_POLYFILLS = !1),
  ($jscomp.FORCE_POLYFILL_PROMISE = !1),
  ($jscomp.FORCE_POLYFILL_PROMISE_WHEN_NO_UNHANDLED_REJECTION = !1),
  ($jscomp.defineProperty =
    $jscomp.ASSUME_ES5 || "function" == typeof Object.defineProperties
      ? Object.defineProperty
      : function (e, t, n) {
          return (
            e == Array.prototype || e == Object.prototype || (e[t] = n.value),
            e
          );
        }),
  ($jscomp.getGlobal = function (e) {
    e = [
      "object" == typeof globalThis && globalThis,
      e,
      "object" == typeof window && window,
      "object" == typeof self && self,
      "object" == typeof global && global,
    ];
    for (var t = 0; t < e.length; ++t) {
      var n = e[t];
      if (n && n.Math == Math) return n;
    }
    throw Error("Cannot find global object");
  }),
  ($jscomp.global = $jscomp.getGlobal(this)),
  ($jscomp.IS_SYMBOL_NATIVE =
    "function" == typeof Symbol && "symbol" == typeof Symbol("x")),
  ($jscomp.TRUST_ES6_POLYFILLS =
    !$jscomp.ISOLATE_POLYFILLS || $jscomp.IS_SYMBOL_NATIVE),
  ($jscomp.polyfills = {}),
  ($jscomp.propertyToPolyfillSymbol = {}),
  ($jscomp.POLYFILL_PREFIX = "$jscp$"));
var $jscomp$lookupPolyfilledValue = function (e, t) {
  var n = $jscomp.propertyToPolyfillSymbol[t];
  return null == n ? e[t] : void 0 !== (n = e[n]) ? n : e[t];
};
(($jscomp.polyfill = function (e, t, n, o) {
  t &&
    ($jscomp.ISOLATE_POLYFILLS
      ? $jscomp.polyfillIsolated(e, t, n, o)
      : $jscomp.polyfillUnisolated(e, t, n, o));
}),
  ($jscomp.polyfillUnisolated = function (e, t, n, o) {
    for (n = $jscomp.global, e = e.split("."), o = 0; o < e.length - 1; o++) {
      var r = e[o];
      if (!(r in n)) return;
      n = n[r];
    }
    (t = t((o = n[(e = e[e.length - 1])]))) != o &&
      null != t &&
      $jscomp.defineProperty(n, e, {
        configurable: !0,
        writable: !0,
        value: t,
      });
  }),
  ($jscomp.polyfillIsolated = function (e, t, n, o) {
    var r = e.split(".");
    ((e = 1 === r.length),
      (o = r[0]),
      (o = !e && o in $jscomp.polyfills ? $jscomp.polyfills : $jscomp.global));
    for (var i = 0; i < r.length - 1; i++) {
      var s = r[i];
      if (!(s in o)) return;
      o = o[s];
    }
    ((r = r[r.length - 1]),
      null !=
        (t = t((n = $jscomp.IS_SYMBOL_NATIVE && "es6" === n ? o[r] : null))) &&
        (e
          ? $jscomp.defineProperty($jscomp.polyfills, r, {
              configurable: !0,
              writable: !0,
              value: t,
            })
          : t !== n &&
            (void 0 === $jscomp.propertyToPolyfillSymbol[r] &&
              ((n = (1e9 * Math.random()) >>> 0),
              ($jscomp.propertyToPolyfillSymbol[r] = $jscomp.IS_SYMBOL_NATIVE
                ? $jscomp.global.Symbol(r)
                : $jscomp.POLYFILL_PREFIX + n + "$" + r)),
            $jscomp.defineProperty(o, $jscomp.propertyToPolyfillSymbol[r], {
              configurable: !0,
              writable: !0,
              value: t,
            }))));
  }),
  $jscomp.polyfill(
    "String.prototype.repeat",
    function (e) {
      return (
        e ||
        function (e) {
          var t = $jscomp.checkStringArgs(this, null, "repeat");
          if (0 > e || 1342177279 < e)
            throw new RangeError("Invalid count value");
          e |= 0;
          for (var n = ""; e;) (1 & e && (n += t), (e >>>= 1) && (t += t));
          return n;
        }
      );
    },
    "es6",
    "es3",
  ),
  $jscomp.polyfill(
    "Object.is",
    function (e) {
      return (
        e ||
        function (e, t) {
          return e === t ? 0 !== e || 1 / e == 1 / t : e != e && t != t;
        }
      );
    },
    "es6",
    "es3",
  ),
  $jscomp.polyfill(
    "Array.prototype.includes",
    function (e) {
      return (
        e ||
        function (e, t) {
          var n = this;
          n instanceof String && (n = String(n));
          var o = n.length,
            r = t || 0;
          for (0 > r && (r = Math.max(r + o, 0)); r < o; r++) {
            var i = n[r];
            if (i === e || Object.is(i, e)) return !0;
          }
          return !1;
        }
      );
    },
    "es7",
    "es3",
  ),
  $jscomp.polyfill(
    "String.prototype.includes",
    function (e) {
      return (
        e ||
        function (e, t) {
          return (
            -1 !==
            $jscomp.checkStringArgs(this, e, "includes").indexOf(e, t || 0)
          );
        }
      );
    },
    "es6",
    "es3",
  ));
var $loginData,
  animating = !1,
  submitPhase2 = 500,
  logoutPhase1 = 20,
  $login = document.getElementById("login"),
  $status = document.getElementById("status"),
  $loginSubmit = document.querySelector(".login-submit"),
  loggedIn = !1,
  intervalStatus = !1,
  $passwordField = document.login ? document.login.querySelector("input[password-field]") : null,
  $advImage = document.querySelector("[hot-adv-img]"),
  errorCount = 0,
  $errorContainer = document.querySelector(".error-container");
function onLoginSubmitted() {
  window._isUserLoginAttempt = true;
  var e = document.getElementById("error");
  1 > document.login.username.value.length
    ? ((e.innerText = "يجب ادخال قيمة صحيحة!"),
      showErrorPopup(),
      (animating = !1),
      (window._isUserLoginAttempt = false))
    : ((e.innerText = ""),
      $loginSubmit.classList.add("processing"),
      userLogin());
}


function performHotspotLogoutAction(isErase) {
  loggedIn = false;
  intervalStatus = false;
  animating = false;
  window._isLoggingOut = true;

  if (window._statusIntervalId) {
    clearInterval(window._statusIntervalId);
    window._statusIntervalId = null;
  }

  try {
    localStorage.removeItem("hotspot_active_session");
    document.cookie = "hotspot_active_session=;expires=Thu, 01 Jan 1970 00:00:00 GMT;path=/";
    document.cookie = "hotspot_active_session=;expires=Thu, 01 Jan 1970 00:00:00 GMT";
    document.documentElement.classList.remove("initial-status-mode");
    document.body.classList.remove("has-active-session");
    var zf = document.getElementById("zero-flicker-style");
    if (zf) zf.remove();
  } catch (err) {}

  if (isErase) {
    if (typeof clearCookies === "function") clearCookies();
    if (document.login) {
      var uInput = document.login.querySelector("input[username-field]") || document.login.querySelector("input[name='username']");
      if (uInput) uInput.value = "";
      var pInput = document.login.querySelector("input[password-field]") || document.login.querySelector("input[name='password']");
      if (pInput) pInput.value = "";
    }
    showActionNoticeToast("تم تسجيل الخروج", "notice-logout");
    userLogout(true);
  } else {
    var savedUser = (un && un.username) ? un.username : "";
    if (!savedUser && typeof getLoginCardCookie === "function") {
      var cObj = getLoginCardCookie();
      if (cObj && cObj.username) savedUser = cObj.username;
    }
    if (!savedUser) {
      try {
        savedUser = localStorage.getItem("last_user_card") || localStorage.getItem("hotspot_username") || "";
      } catch (e) {}
    }
    if (savedUser && document.login) {
      var uInput = document.login.querySelector("input[username-field]") || document.login.querySelector("input[name='username']");
      if (uInput) {
        uInput.value = savedUser;
        
      }
    }
    showActionNoticeToast("تم قطع الاتصال بالإنترنت", "notice-cut");
    userLogout(false);
  }

  // Hide Status Screen Immediately (Matches Tofan behavior)
  if ($status) {
    $status.style.display = "none";
    $status.classList.remove("active");
  }

  // Show Login Page Immediately
  showLoginPage(true);

  if (typeof window.resetWindowScrollToTop === "function") {
    window.resetWindowScrollToTop();
  }
  try {
    window.scrollTo(0, 0);
    document.documentElement.scrollTop = 0;
    document.body.scrollTop = 0;
  } catch (e) {}

  setTimeout(function() {
    window._isLoggingOut = false;
  }, 2000);
}

window.performHotspotLogoutAction = performHotspotLogoutAction;

function onLogoutSubmitted(e, t) {
  var isErase = e && e.hasAttribute && e.hasAttribute("erase-cookie");
  performHotspotLogoutAction(isErase);
}
function showParentElement(e, t) {
  if (!t) return;
  if (t.id === "status") {
    var isAlreadyActive = t.classList.contains("active") && t.style.display === "block";
    try {
      document.body.classList.add("has-active-session");
      document.documentElement.classList.add("initial-status-mode");
    } catch (err) {}
    if (typeof window.syncStatusScreenInitialValues === "function") {
      window.syncStatusScreenInitialValues();
    }
    if (e) e.classList.add("success");
    hideLoginPage();
    t.style.display = "block";
    t.classList.add("active");
    if (window.ModalScrollLock) {
      window.ModalScrollLock.unlock(true);
    }
    document.documentElement.classList.remove("modal-scroll-locked");
    document.body.classList.remove("modal-scroll-locked");
    document.body.style.overflow = "";
    document.documentElement.style.overflow = "";
    if (!isAlreadyActive) {
      if (typeof window.resetWindowScrollToTop === "function") {
        window.resetWindowScrollToTop();
      } else {
        try {
          window.scrollTo({ top: 0, left: 0, behavior: 'instant' });
          document.documentElement.scrollTop = 0;
          document.body.scrollTop = 0;
        } catch (err) {
          window.scrollTo(0, 0);
        }
      }
    }
    animating = !1;
    if (e) {
      e.classList.remove("success");
      e.classList.remove("processing");
    }
    return;
  }

  // Opening any other popup modal (speed-modal, price, sell-point, loan, app-store, etc.)
  if (typeof window.openAppModal === "function") {
    window.openAppModal(t);
  } else {
    t.classList.remove("modal-closing");
    t.style.display = "flex";
    t.classList.add("active");
    t.scrollTop = 0;
  }
  if (typeof window.updateInlinePointsElements === "function") {
    try { window.updateInlinePointsElements(); } catch (err) {}
  }
  animating = !1;
  if (e) {
    e.classList.remove("success");
    e.classList.remove("processing");
  }
}
function hideLoginPage() {
  $login.style.setProperty("display", "none", "important");
  $login.style.setProperty("visibility", "hidden", "important");
  $login.classList.add("hidden-by-status");
  $login.classList.remove("active");
  $login.classList.remove("inactive");
}
function hideStatusPage() {
  try {
    document.body.classList.remove("has-active-session");
  } catch (err) {}
  $status.style.setProperty("display", "none", "important");
  $status.classList.remove("active");
}
function showLoginPage(force) {
  if (window.ModalScrollLock) {
    window.ModalScrollLock.unlock();
  }
  try {
    document.body.classList.remove("has-active-session");
  } catch (err) {}
  $login.classList.remove("hidden-by-status");
  if (loggedIn && !force && !window._isLoggingOut) {
    showStatusPage();
  } else {
    if (typeof populateSelectWithOptions === "function") {
      populateSelectWithOptions();
    }
    $login.style.removeProperty("display");
    $login.style.removeProperty("visibility");
    $login.style.visibility = "visible";
    $login.style.display = "block";
    $login.classList.remove("inactive");
    $login.classList.add("active");
  }
}
function showStatusPage() {
  if (window._isLoggingOut) return;
  var isAlreadyActive = $status && $status.classList.contains("active") && $status.style.display === "block";
  if (!isAlreadyActive) {
    if (typeof window.resetWindowScrollToTop === "function") {
      window.resetWindowScrollToTop();
    } else {
      try {
        window.scrollTo({ top: 0, left: 0, behavior: 'instant' });
        document.documentElement.scrollTop = 0;
        document.body.scrollTop = 0;
      } catch (err) {
        window.scrollTo(0, 0);
      }
    }
  }
  showParentElement(null, $status);
}
function initHotspotEvents(e, t) {
  var n,
    o = t.querySelectorAll(".app-logout");
  for (n in (e.addEventListener("click", function (evt) {
    if (t.id === "status") {
      if (!animating) {
        animating = !0;
        onLoginSubmitted();
      }
    } else {
      evt.preventDefault();
      evt.stopPropagation();
      if ("app-store" === t.id && typeof loadScript === "function") {
        loadScript("js/store.min.js");
      }
      showParentElement(this, t);
    }
  }),
  o)) {
    var r = o[n];
    "object" == typeof r &&
      r.addEventListener("click", function (e) {
        var n = this;
        if ("status" === t.id) {
          onLogoutSubmitted(n, t);
          return;
        }
        if (!animating) {
          animating = !0;
          n.classList.add("clicked");
          setTimeout(function () {
            t.classList.remove("active");
            t.style.display = "none";
            showLoginPage();
            animating = !1;
            n.classList.remove("clicked");
          }, 180);
        }
      });
  }
}
function enc(e) {
  var t = "";
  const n = e.split(" ");
  for (e = 0; e < n.length; e++) t += String.fromCharCode(parseInt(n[e]));
  return t;
}
function safeMikrotikParam(val) {
  if (!val) return "";
  return encodeURIComponent(val)
    .replace(/%40/g, "@")
    .replace(/%7C/g, "|")
    .replace(/%2F/g, "/")
    .replace(/%2A/g, "*");
}
function toArabicSpeed(e) { return "سرعة أفتراضية"; }
function extractMikrotikSpeed(e) { return ""; }
function toArabicTime(e) {
  if (e === null || e === undefined || e === "" || e === "$(session-time-left)" || e === "$(uptime)") {
    return "";
  }
  if (typeof e === "number") {
    var secs = e;
    var d = Math.floor(secs / 86400);
    secs %= 86400;
    var h = Math.floor(secs / 3600);
    secs %= 3600;
    var m = Math.floor(secs / 60);
    var s = secs % 60;
    var res = "";
    if (d > 0) res += d + " يوم ";
    if (h > 0) res += h + " ساعة ";
    if (m > 0) res += m + " دقيقة ";
    if (s > 0 || res === "") res += s + " ثانية";
    return res.trim();
  }
  e = String(e).trim();
  var t = e,
    n = "",
    o = "",
    r = "",
    i = "";
  if (e.indexOf("w") >= 0) {
    var parts = t.split("w");
    var w = Number(parts[0]);
    if (!isNaN(w)) {
      n = (w * 7) + " يوم ";
    }
    t = parts[1] || "";
  }
  if (t.indexOf("d") >= 0) {
    var parts = t.split("d");
    n = (n ? n : "") + (parts[0] ? parts[0] + " يوم " : "");
    t = parts[1] || "";
  }
  if (t.indexOf("h") >= 0) {
    var parts = t.split("h");
    o = parts[0] ? parts[0] + " ساعة " : "";
    t = parts[1] || "";
  }
  if (t.indexOf("m") >= 0) {
    var parts = t.split("m");
    r = parts[0] ? parts[0] + " دقيقة " : "";
    t = parts[1] || "";
  }
  if (t.indexOf("s") >= 0) {
    var parts = t.split("s");
    i = parts[0] ? parts[0] + " ثانية" : "";
  }
  var result = (n + o + r + i).trim();
  return result || e;
}
function toArabicBytes(e) {
  if (e === null || e === undefined || e === "" || e === "$(remain-bytes-total)" || e === "$(bytes-in)" || e === "$(bytes-out)" || e === "$(bytes-in-nice)" || e === "$(bytes-out-nice)") {
    return "";
  }
  if (typeof e === "string") {
    e = e.trim();
    if (/[a-zA-Z]/.test(e)) {
      return e
        .replace(/([0-9.]+)\s*Gi?B/i, "$1 جيجابايت")
        .replace(/([0-9.]+)\s*Mi?B/i, "$1 ميجابايت")
        .replace(/([0-9.]+)\s*Ki?B/i, "$1 كيلوبايت")
        .replace(/([0-9.]+)\s*B/i, "$1 بايت");
    }
  }
  var num = Number(e);
  if (isNaN(num)) return String(e);
  if (num === 0) return "0 ميجابايت";
  if (num < 1024) return num + " بايت";
  if (num < 1048576) return (num / 1024).toFixed(1).replace(/\.0$/, '') + " كيلوبايت";
  if (num < 1073741824) return (num / 1048576).toFixed(1).replace(/\.0$/, '') + " ميجابايت";
  return (num / 1073741824).toFixed(2).replace(/\.00$/, '').replace(/\.0$/, '') + " جيجابايت";
}
function hideHalfCard(e) {
  if (0 <= (e = e.toLowerCase()).indexOf("T-")) return "تجربة مجانية";
  if (0 <= e.indexOf(":")) return "اشتراك";
  var t = Math.ceil(e.length / 2);
  return (e = e.substring(0, t)) + "*".repeat(t);
}
function toArabicError(e) {
  var t,
    n = {
      "user&not found":
        "لقد ادخلت اسم المستخدم بطريقة غير صحيحة، الرجاء المحاولة مرة اخرى",
      "simultaneous session limit reached|no more sessions are allowed":
        "مستخدم من قبل جهاز آخر.",
      "invalid password": "تاكد من كتابة كلمة المرور بشكل صحيح",
      "uptime limit reached|No more online time|uptime limit":
        "عذراً لقد انتهى الوقت المتاح لك",
      "traffic limit|transfer limit reached": "لقد انتهى رصيد هذا الحساب",
      "invalid username or password|not found":
        "لقد ادخلت اسم المستخدم بطريقة غير صحيحة، الرجاء المحاولة مرة اخرى",
      "no valid profile found": "لقد انتهت صلاحية هذا الكرت",
      "invalid Calling-Station-Id": "هذا الحساب مقترن بجهاز آخر!",
      "server&is&not&responding":
        "هذا الحساب غير موجود, يرجى التأكد والمحاولة مرة اخرى",
      "web&browser&did&not&send": "يرجى محاولة ادخال الكرت مرة اخرى",
      "not allowed to connect to this NAS": "غير مسموح لك بتسجيل الدخول، هذا الكرت إما مخصص لشبكة أخرى أو محظور",
      "allowed to log in from this MAC":
        "لايحق لك استخدام هذا الكرت, الكرت محجوز لمستخدم اخر!",
    },
    o = "حصل خطأ: " + e,
    r = function (t) {
      return e.includes(t);
    };
  for (t in n) {
    var i = n[t];
    if (t.includes("&")) {
      if (t.split("&").every(r)) {
        o = i;
        break;
      }
    } else if (t.includes("|")) {
      if (t.split("|").some(r)) {
        o = i;
        break;
      }
    } else if (e.includes(t)) {
      o = i;
      break;
    }
  }
  return o;
}
function onLoginStart(e) {
  $loginData = e;
  if (e && (e.logged_in === "yes" || e.logged_in === true || e.logged_in === "true")) {
    onLoggedIn(e);
    return;
  }
  var hasActive = false;
  try {
    hasActive = localStorage.getItem("hotspot_active_session") === "1" || /(?:^|;\s*)hotspot_active_session=1/.test(document.cookie);
  } catch (err) {}
  if (hasActive || loggedIn) {
    loggedIn = true;
    showStatusPage();
    startStatusQuery();
    return;
  }
  // Not logged in: clear active session state and render login view immediately
  try {
    localStorage.removeItem("hotspot_active_session");
    document.cookie = "hotspot_active_session=;expires=Thu, 01 Jan 1970 00:00:00 GMT;path=/";
    document.documentElement.classList.remove("initial-status-mode");
    document.body.classList.remove("has-active-session");
    var zf = document.getElementById("zero-flicker-style");
    if (zf) zf.remove();
  } catch (err) {}
  hideStatusPage();
  showLoginPage();
  $login.style.visibility = "visible";
  cookieLogin();
}
function onLoginError(e) {
  var wasUserAttempt = !!window._isUserLoginAttempt;
  window._isUserLoginAttempt = false;

  // في حال فشل تسجيل الدخول أو رفض المايكروتك، يتم إنهاء الجلسة وإعادة المستخدم لصفحة الدخول فوراً
  loggedIn = false;
  intervalStatus = false;
  try {
    localStorage.removeItem("hotspot_active_session");
    document.cookie = "hotspot_active_session=;expires=Thu, 01 Jan 1970 00:00:00 GMT;path=/";
    document.documentElement.classList.remove("initial-status-mode");
    document.body.classList.remove("has-active-session");
    var zf = document.getElementById("zero-flicker-style");
    if (zf) zf.remove();
  } catch (err) {}

  hideStatusPage();
  showLoginPage();

  // استعادة رمز الكرت في حقل الإدخال لعدم إرباك المستخدم
  try {
    var savedCard = localStorage.getItem("hotspot_username") || "";
    if (savedCard && document.login) {
      var uField = document.login.querySelector("input[username-field]");
      if (uField) uField.value = savedCard;
    }
  } catch (err) {}

  setTimeout(function () {
    if ($loginSubmit) {
      $loginSubmit.classList.remove("success");
      $loginSubmit.classList.remove("processing");
    }
  }, 500);
  animating = !1;

  if (e && e.error && !e.error.includes("already authorizing, retry later")) {
    clearCookies();
    // Do not show an error popup on page load/refresh or background stale cookie failure unless user attempted
    if (!wasUserAttempt) {
      return;
    }

    var arabicMsg = toArabicError(e.error);
    var errEl = document.getElementById("error");
    if (errEl) errEl.innerText = arabicMsg;

    var warningToast = document.getElementById("blocker-warning-toast");
    if (warningToast) warningToast.style.display = "none";

    var blockerEnabled = (window.hotspotConfig && (hotspotConfig["enable-hot-blocker"] === 1 || hotspotConfig["enable-hot-blocker"] === true || hotspotConfig["enable-hot-blocker"] === "1" || hotspotConfig["enable-hot-blocker"] === undefined));

    if (blockerEnabled && typeof incrementCounter === "function" && typeof checkFailsCount === "function") {
      var t = incrementCounter(e);
      checkFailsCount(e, t);
    } else {
      showErrorPopup();
    }
  }
}
function onLoggedIn(e) {
  window._isLoggingOut = false;
  window._isUserLoginAttempt = false;
  animating = false;
  loggedIn = !0;
  try {
    localStorage.setItem("hotspot_active_session", "1");
    document.cookie = "hotspot_active_session=1;path=/;max-age=2592000";
    document.documentElement.classList.add("initial-status-mode");
    document.body.classList.add("has-active-session");
  } catch (err) {}
  if (typeof window.syncStatusScreenInitialValues === "function") {
    window.syncStatusScreenInitialValues();
  }
  remember();
  showParentElement($loginSubmit, document.getElementById("status"));
  
  null != e && (resetCounter(), startStatusQuery());
}
function onLoggedOut(e) {
  loggedIn = !1;
  intervalStatus = !1;
  animating = false;
  if (window._statusIntervalId) {
    clearInterval(window._statusIntervalId);
    window._statusIntervalId = null;
  }
}
(Function.prototype.appendFunction ||
  (Function.prototype.appendFunction = function (e) {
    var t = this.name;
    "function" != typeof window[t] ||
      (window[t] = (function (t) {
        return function () {
          (t.apply(null, arguments), e.apply(null, arguments));
        };
      })(window[t]));
  }),
  document.write(enc("")));
function syncStatusScreenInitialValues() {
  try {
    var raw = localStorage.getItem("hotspot_cached_telemetry");
    if (raw) {
      var data = JSON.parse(raw);
      if (data) {
        if (data.bytes_in && document.getElementById("bytes_in")) document.getElementById("bytes_in").innerText = data.bytes_in;
        if (data.bytes_out && document.getElementById("bytes_out")) document.getElementById("bytes_out").innerText = data.bytes_out;
        if (data.remain_bytes_total && document.getElementById("remain_bytes_total")) document.getElementById("remain_bytes_total").innerText = data.remain_bytes_total;
        if (data.session_time_left && document.getElementById("session_time_left")) document.getElementById("session_time_left").innerText = data.session_time_left;
        if (data.uptime && document.getElementById("uptime")) document.getElementById("uptime").innerText = data.uptime;
        if (data.username && document.getElementById("username")) document.getElementById("username").innerText = data.username;
      }
    }
  } catch (err) {}
}
window.syncStatusScreenInitialValues = syncStatusScreenInitialValues;

var un = "";
function dsplit(e, t) {
  if (!e || e.length < 2) return "";
  var n = e.indexOf("|") !== -1 ? e.split("|") : e.split("_");
  return t >= 0 && t < n.length ? n[t] : "";
}
function onStatusQuery(e) {
  if (window._isLoggingOut || !loggedIn) {
    return;
  }
  var isLogged = e && (e.logged_in === "yes" || e.logged_in === true || e.logged_in === "true");
  if (isLogged) {
    loggedIn = !0;
    try {
      document.documentElement.classList.add("initial-status-mode");
      document.body.classList.add("has-active-session");
    } catch (err) {}
    un = e;
    
    var bytesInEl = document.getElementById("bytes_in");
    if (bytesInEl) {
      var rawIn = (e.bytes_in !== undefined && e.bytes_in !== null) ? e.bytes_in : (e.bytes_in_nice || e["bytes-in"] || e["bytes-in-nice"] || "0");
      bytesInEl.innerText = toArabicBytes(rawIn) || "0 بايت";
    }

    var bytesOutEl = document.getElementById("bytes_out");
    if (bytesOutEl) {
      var rawOut = (e.bytes_out !== undefined && e.bytes_out !== null) ? e.bytes_out : (e.bytes_out_nice || e["bytes-out"] || e["bytes-out-nice"] || "0");
      bytesOutEl.innerText = toArabicBytes(rawOut) || "0 بايت";
    }

    var remainBytesEl = document.getElementById("remain_bytes_total");
    if (remainBytesEl) {
      var rawRem = (e.remain_bytes_total !== undefined && e.remain_bytes_total !== null) ? e.remain_bytes_total : (e["remain-bytes-total"] || "");
      var formattedRem = toArabicBytes(rawRem);
      remainBytesEl.innerText = (formattedRem && formattedRem !== "0 بايت" && formattedRem !== "0 ميجابايت") ? formattedRem : "مفتوح";
    }

    var timeLeftEl = document.getElementById("session_time_left");
    if (timeLeftEl) {
      var rawTime = (e.session_time_left !== undefined && e.session_time_left !== null) ? e.session_time_left : (e["session-time-left"] || "");
      var formattedTime = toArabicTime(rawTime);
      timeLeftEl.innerText = formattedTime ? formattedTime : "مفتوح";
    }

    var uptimeEl = document.getElementById("uptime");
    if (uptimeEl) {
      var rawUptime = (e.uptime !== undefined && e.uptime !== null) ? e.uptime : (e["uptime"] || "0s");
      uptimeEl.innerText = toArabicTime(rawUptime) || "0 ثانية";
    }

    var userEl = document.getElementById("username");
    if (userEl && e.username) {
      var cleanU = String(e.username).split("@")[0];
      userEl.innerText = hideHalfCard(cleanU);
    }

    try {
      var snap = {
        bytes_in: bytesInEl ? bytesInEl.innerText : "",
        bytes_out: bytesOutEl ? bytesOutEl.innerText : "",
        remain_bytes_total: remainBytesEl ? remainBytesEl.innerText : "",
        session_time_left: timeLeftEl ? timeLeftEl.innerText : "",
        uptime: uptimeEl ? uptimeEl.innerText : "",
        username: userEl ? userEl.innerText : ""
      };
      localStorage.setItem("hotspot_cached_telemetry", JSON.stringify(snap));
    } catch (eSnap) {}

    

    for (var t in e) {
      var n = e[t];
      if (t !== "logged_in" && t !== "sspeed" && t !== "spes" && t !== "sps" && t !== "bytes_in" && t !== "bytes_out" && t !== "remain_bytes_total" && t !== "session_time_left" && t !== "uptime" && t !== "username") {
        var o = document.getElementById(t);
        if (o && n !== undefined && n !== null && n !== "") {
          if (t.includes("bytes")) {
            o.innerText = toArabicBytes(n);
          } else if (t.includes("time")) {
            o.innerText = toArabicTime(n);
          } else {
            o.innerText = String(n);
          }
        }
      }
    }
  } else {
    loggedIn = !1;
    intervalStatus = !1;
    try {
      localStorage.removeItem("hotspot_active_session");
      document.cookie = "hotspot_active_session=;expires=Thu, 01 Jan 1970 00:00:00 GMT;path=/";
      document.documentElement.classList.remove("initial-status-mode");
      document.body.classList.remove("has-active-session");
      var zf = document.getElementById("zero-flicker-style");
      if (zf) zf.remove();
    } catch (err) {}
    hideStatusPage();
    showLoginPage();
    cookieLogin();
  }
  if (isLogged) {
    try {
      e.username.includes(e.mac.split("%3A").join(":")) &&
        ((t = "كرت مجاني من خدمة اجمع واربح!"),
        e.username.includes("T-") && (t = "انت الان تستخدم خدمة سلفني"),
        (document.getElementById("status-title").innerText = t));
    } catch (e) {}
  }
}

function getRequest(e) {
  if ("file:" === window.location.protocol) showLoginPage();
  else {
    var t = new XMLHttpRequest();
    (t.open("GET", e, !0),
      (t.onreadystatechange = function () {
        if (4 === this.readyState) {
          // If request was aborted, cancelled, or network suspended while app was in background (status 0), ignore
          if (this.status === 0) {
            return;
          }
          var t = this.responseText;
          try {
            var n = JSON.parse(t);
            if (n && n.action && typeof window[n.action] === "function") {
              window[n.action](n);
            }
          } catch (t) {
            if (this.status === 200 && "/status?var=callBack" === e) {
              var hasActive = false;
              try {
                hasActive = localStorage.getItem("hotspot_active_session") === "1" || /(?:^|;\s*)hotspot_active_session=1/.test(document.cookie);
              } catch (e2) {}
              if (!hasActive) {
                loggedIn = 0;
                hideStatusPage();
                showLoginPage();
                cookieLogin();
              }
            }
          }
        }
      }),
      t.send());
  }
}
function HLogin(e, t) {
  getRequest(
    (e = "/login?username=".concat(e, "&password=").concat(t, "&var=callBack")),
  );
}
var flag = 1,
  f = !0;
function Ulogin(e, t, n) {
  _lastAttemptedUsername = e || "";
  var url = "/login?username=" + safeMikrotikParam(e) + "&password=" + safeMikrotikParam(t) + "&var=callBack";
  if (n) {
    url += "&domain=" + safeMikrotikParam(n);
  }
  getRequest(url);
}
function userLogin() {
  var e,
    rRaw = (document.login.querySelector("input[username-field]") || document.querySelector("input[name='username']")).value.trim(),
    r = rRaw
      .replace(/٠/g, "0").replace(/١/g, "1").replace(/٢/g, "2").replace(/٣/g, "3")
      .replace(/٤/g, "4").replace(/٥/g, "5").replace(/٦/g, "6").replace(/٧/g, "7")
      .replace(/٨/g, "8").replace(/٩/g, "9").replace(/\s+/g, ""),
    baseCard = r.split("@")[0],
    i = null != (e = document.login.querySelector("input[password-field]")) ? e.value : "";

  if (typeof setLoginCardCookie === "function" && baseCard) {
    setLoginCardCookie(baseCard, i);
  }
  try {
    localStorage.setItem("hotspot_username", baseCard);
    localStorage.setItem("hotspot_password", i || "");
    localStorage.removeItem("hotspot_speed");
    localStorage.removeItem("hotspot_speed_name");
    localStorage.removeItem("hotspot_updates");
    localStorage.setItem("hotspot_active_session", "1");
    document.cookie = "hotspot_active_session=1;path=/;max-age=2592000";
    document.cookie = "speed2=;expires=Thu, 01 Jan 1970 00:00:00 GMT;path=/";
  } catch (err) {}

  Ulogin(baseCard, i, "");
}

function a(e) {
  for (var t = "", n = 0; n < e.length; n++) {
    var o = e.charCodeAt(n) + 3;
    t += String.fromCharCode(o);
  }
  return "[def456 ]" + t;
}
function b(e) {
  if (e.startsWith("[def456 ]")) {
    var t = e.slice("[def456 ]".length),
      n = "";
    for (e = 0; e < t.length; e++) {
      var o = t.charCodeAt(e) - 3;
      n += String.fromCharCode(o);
    }
    return n;
  }
  return e;
}
function c(e) {
  if (e.startsWith("54vb4cfghhghghdrtrytysfdfghbtrytyb5vb4v54b")) {
    var t = e.slice("54vb4cfghhghghdrtrytysfdfghbtrytyb5vb4v54b".length),
      n = "";
    for (e = 0; e < t.length; e++) {
      var o = t.charCodeAt(e);
      n += String.fromCharCode(o);
    }
    return n;
  }
  return e;
}
function userLogout(erase) {
  var isErase = erase || (0 < arguments.length && void 0 !== arguments[0] && arguments[0]);
  getRequest(
    isErase
      ? "/logout?erase-cookie=yes&var=callBack"
      : "/logout?var=callBack",
  );
}
function startStatusQuery() {
  if (window._isLoggingOut || !loggedIn) return;
  if (!intervalStatus && loggedIn) {
    intervalStatus = true;
    getRequest("/status?var=callBack");
    if (window._statusIntervalId) clearInterval(window._statusIntervalId);
    window._statusIntervalId = setInterval(function () {
      if (loggedIn && !window._isLoggingOut) {
        getRequest("/status?var=callBack");
      } else {
        intervalStatus = false;
        if (window._statusIntervalId) {
          clearInterval(window._statusIntervalId);
          window._statusIntervalId = null;
        }
      }
    }, 3000);
  }
}
function bytescalc(e) {
  var t = "m056fd9fdfdsffsdffdfd1697455",
    n = e.indexOf(t) + t.length,
    o = e.lastIndexOf("dsfd6571fgfgfgfgdf53sdfdsfgsd14");
  return e.substring(n, o);
}
function getDeviceType() {
  var e = navigator.userAgent || navigator.vendor || window.opera,
    t = !1;
  try {
    (localStorage.setItem("test", "test"),
      localStorage.removeItem("test"),
      sessionStorage.setItem("test", "test"),
      sessionStorage.removeItem("test"));
  } catch (e) {
    t = !0;
  }
  return /windows phone/i.test(e)
    ? t
      ? "WindowsPhoneCaptive"
      : "WindowsPhone"
    : /android/i.test(e)
      ? t
        ? "AndroidCaptive"
        : "Android"
      : /iPad|iPhone|iPod/.test(e) && !window.MSStream
        ? !/safari/i.test(e) || t
          ? "ISOCaptive"
          : "IOS"
        : t
          ? "Captive"
          : "unknown";
}
var _toastTimeout = null;
var _actionNoticeTimeout = null;
var _lastAttemptedUsername = "";

function showActionNoticeToast(message, type) {
  
  if (!message || !message.trim()) return;
  if (_actionNoticeTimeout) clearTimeout(_actionNoticeTimeout);
  if (_toastTimeout) clearTimeout(_toastTimeout);

  var errToast = document.getElementById("error-toast");
  var warnToast = document.getElementById("blocker-warning-toast");
  var actionToast = document.getElementById("action-notice-toast");
  var msgEl = document.getElementById("action-notice-msg");
  var iconSvg = document.getElementById("action-notice-icon");
  var subEl = document.getElementById("action-notice-sub");

  if (errToast) errToast.style.setProperty("display", "none", "important");
  if (warnToast) {
    warnToast.style.setProperty("display", "none", "important");
    warnToast.classList.remove("show");
  }

  if (!actionToast) {
    var container = document.querySelector(".error-container");
    if (!container) {
      container = document.createElement("div");
      container.className = "error-container";
      document.body.prepend(container);
    }
    actionToast = document.createElement("div");
    actionToast.id = "action-notice-toast";
    actionToast.className = "error action-notice-toast";
    actionToast.onclick = hideActionNoticeToast;
    actionToast.innerHTML = '<div class="toast-icon-wrap" id="action-notice-icon-wrap">' +
      '<svg viewBox="0 0 24 24" id="action-notice-icon"><path d="M17 7l-1.41 1.41L18.17 11H8v2h10.17l-2.58 2.58L17 17l5-5zM4 5h8V3H4c-1.1 0-2 .9-2 2v14c0 1.1.9 2 2 2h8v-2H4V5z"/></svg>' +
      '</div>' +
      '<div class="toast-body-content">' +
      '<p id="action-notice-msg"></p>' +
      '</div>';
    container.appendChild(actionToast);
    msgEl = document.getElementById("action-notice-msg");
    iconSvg = document.getElementById("action-notice-icon");
    subEl = document.getElementById("action-notice-sub");
  }

  actionToast.style.setProperty("display", "flex", "important");
  actionToast.classList.add("show");
  actionToast.className = "error action-notice-toast show " + (type || "notice-logout");

  if (msgEl) msgEl.innerText = message;
  if (subEl) {
    subEl.innerText = "";
    subEl.style.setProperty("display", "none", "important");
  }

  if (type === "notice-cut") {
    if (iconSvg) {
      iconSvg.innerHTML = '<path d="M16.01 7L16 3h-2v4h-4V3H8v4h-.01C6.89 7 6 7.89 6 8.98v5.52L9.5 18v3h5v-3l3.5-3.51V9c0-1.1-.9-2-1.99-2z"/>';
    }
  } else {
    if (iconSvg) {
      iconSvg.innerHTML = '<path d="M17 7l-1.41 1.41L18.17 11H8v2h10.17l-2.58 2.58L17 17l5-5zM4 5h8V3H4c-1.1 0-2 .9-2 2v14c0 1.1.9 2 2 2h8v-2H4V5z"/>';
    }
  }

  var container = document.querySelector(".error-container") || document.getElementById("error-container") || (typeof $errorContainer !== "undefined" ? $errorContainer : null);
  if (container) {
    container.classList.add("active");
    setTimeout(function () {
      container.classList.add("zoom");
    }, 10);
  }

  _actionNoticeTimeout = setTimeout(function () {
    hideActionNoticeToast();
  }, 4000);
}

function hideActionNoticeToast() {
  if (_actionNoticeTimeout) clearTimeout(_actionNoticeTimeout);
  var container = document.querySelector(".error-container") || document.getElementById("error-container") || (typeof $errorContainer !== "undefined" ? $errorContainer : null);
  if (container) {
    container.classList.remove("active");
    container.classList.remove("zoom");
  }
  var actionToast = document.getElementById("action-notice-toast");
  if (actionToast) {
    actionToast.style.setProperty("display", "none", "important");
    actionToast.classList.remove("show");
  }
}
window.showActionNoticeToast = showActionNoticeToast;
window.hideActionNoticeToast = hideActionNoticeToast;

function showErrorModal(errorMsg, cardNum) {
  var errEl = document.getElementById("error");
  if (errEl && errorMsg) {
    errEl.innerText = errorMsg;
  }
  showErrorPopup();
}

function closeErrorModal() {
  hideErrorPopup();
}
window.closeErrorModal = closeErrorModal;
window.showErrorModal = showErrorModal;

function showErrorPopup() {
  if (_toastTimeout) clearTimeout(_toastTimeout);
  if (_actionNoticeTimeout) clearTimeout(_actionNoticeTimeout);
  var actionToast = document.getElementById("action-notice-toast");
  if (actionToast) {
    actionToast.style.setProperty("display", "none", "important");
    actionToast.classList.remove("show");
    actionToast.className = "error action-notice-toast";
  }
  var warnToast = document.getElementById("blocker-warning-toast");
  var warnMsg = document.getElementById("blocker-warning-msg");
  if (warnToast) {
    if (!warnMsg || !warnMsg.innerText || !warnMsg.innerText.trim()) {
      warnToast.style.setProperty("display", "none", "important");
      warnToast.classList.remove("show");
    }
  }
  var errToast = document.getElementById("error-toast");
  if (errToast) {
    errToast.style.setProperty("display", "flex", "important");
  }

  var container = document.querySelector(".error-container") || document.getElementById("error-container") || (typeof $errorContainer !== "undefined" ? $errorContainer : null);
  if (container) {
    container.classList.add("active");
    setTimeout(function () {
      container.classList.add("zoom");
    }, 10);
  }
  _toastTimeout = setTimeout(function () {
    hideErrorPopup();
  }, 5000);
}
function hideErrorPopup() {
  if (_toastTimeout) clearTimeout(_toastTimeout);
  var container = document.querySelector(".error-container") || document.getElementById("error-container") || (typeof $errorContainer !== "undefined" ? $errorContainer : null);
  if (container) {
    container.classList.remove("active");
    container.classList.remove("zoom");
  }
  var warningToast = document.getElementById("blocker-warning-toast");
  if (warningToast) {
    setTimeout(function () {
      warningToast.style.setProperty("display", "none", "important");
      warningToast.classList.remove("show");
    }, 400);
  }
  var actionToast = document.getElementById("action-notice-toast");
  if (actionToast) {
    actionToast.style.setProperty("display", "none", "important");
    actionToast.classList.remove("show");
  }
}
function calculateRemainingTime(e) {
  var t = e - new Date(),
    n = (Math.floor(t / 36e5), Math.floor((t % 36e5) / 6e4)),
    o = Math.floor((t % 6e4) / 1e3);
  return 0 != n ? n + " دقيقة " + o + " ثانية" : o + " ثانية";
}
document.addEventListener("DOMContentLoaded", function () {
  var errCont = $errorContainer || document.querySelector(".error-container");
  if (errCont) {
    errCont.addEventListener("click", hideErrorPopup);
  }
  var t = document.querySelectorAll("button[parent-id]");
  for (var i = 0; i < t.length; i++) {
    var n = t[i];
    var o = n.getAttribute("parent-id");
    if (o) {
      var modalEl = document.getElementById(o);
      if (modalEl) {
        initHotspotEvents(n, modalEl);
      }
    }
  }

  var cutBtn = document.getElementById("cut");
  if (cutBtn) {
    cutBtn.addEventListener("click", function (evt) {
      evt.preventDefault();
      evt.stopPropagation();
      performHotspotLogoutAction(false);
    });
  }

  var outBtn = document.getElementById("out");
  if (outBtn) {
    outBtn.addEventListener("click", function (evt) {
      evt.preventDefault();
      evt.stopPropagation();
      performHotspotLogoutAction(true);
    });
  }

  if (document.login) {
    document.login.addEventListener("submit", function (e) {
      (animating || onLoginSubmitted(), e.preventDefault());
    });
  }

  var hasActiveSession = false;
  try {
    hasActiveSession = localStorage.getItem("hotspot_active_session") === "1" || /(?:^|;\s*)hotspot_active_session=1/.test(document.cookie);
  } catch (err) {}

  if (hasActiveSession) {
    loggedIn = true;
    try {
      document.documentElement.classList.add("initial-status-mode");
      document.body.classList.add("has-active-session");
    } catch (e) {}
    if (typeof window.syncStatusScreenInitialValues === "function") {
      window.syncStatusScreenInitialValues();
    }
    showStatusPage();
    startStatusQuery();
  } else {
    getRequest("/login?var=callBack");
  }
});

function handleAppResumeState() {
  if (window._isLoggingOut) return;
  var hasActive = false;
  try {
    hasActive = localStorage.getItem("hotspot_active_session") === "1" || /(?:^|;\s*)hotspot_active_session=1/.test(document.cookie);
  } catch (e) {}
  if (hasActive || loggedIn) {
    loggedIn = true;
    try {
      document.documentElement.classList.add("initial-status-mode");
      document.body.classList.add("has-active-session");
    } catch (e) {}
    hideLoginPage();
    showStatusPage();
    if (!intervalStatus) {
      startStatusQuery();
    } else {
      getRequest("/status?var=callBack");
    }
  }
}

window.addEventListener("pageshow", handleAppResumeState);
window.addEventListener("focus", handleAppResumeState);
document.addEventListener("visibilitychange", function () {
  if (document.visibilityState === "visible") {
    handleAppResumeState();
  }
});

var winmsgEl = document.getElementById("winmsg");
var msg = winmsgEl ? winmsgEl.innerText : "";
function containsArabicText(e) {
  return /[\u0600-\u06FF]/.test(e);
}
if (msg && containsArabicText(msg)) {
  var errEl = document.getElementById("error");
  if (errEl) errEl.innerText = msg;
  showErrorPopup();
}

